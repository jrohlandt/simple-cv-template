Usage:

1. Copy and rename /data/sample-data.json and replace data with your own.
2. Serve with a local webserver. E.g. python -m http.server 3000
3. Open with a web browser like Chrome and then use the print feature to save as PDF.
